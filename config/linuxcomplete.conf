{
    "max_candidates": 5,
    "min_prefix_length": 2,
    "learn_new_words": true,

    "ai_rerank_enabled": false,
    "ai_smart_fallback": true,
    "ai_debug_logging": false,
    "ai_model": "gpt-4o-mini",
    "ai_timeout_ms": 1200,
    "ai_uncertainty_gap_threshold": 160,
    "ai_max_cache_entries": 128,

    "languages": ["en"],
    "trigger_key": "Control+Space",
    "accept_key": "Tab",
    "dismiss_key": "Escape",

    "__comment_disabled_programs":
        "SmartComplete stays out of the way for these programs. Uncomment the array below to REPLACE the default list, or use 'disabled_programs_extend' to add apps while keeping the defaults. Default blocklist already covers the common terminal emulators and shells (kitty, alacritty, foot, wezterm, gnome-terminal, konsole, xterm, urxvt, rxvt, st, xfce4-terminal, lxterminal, mate-terminal, deepin-terminal, terminator, tilix, hyper, terminology, blackbox, ptyxis, cool-retro-term, termite, zsh, bash, fish, tmux, screen).",

    "disabled_programs_extend": []
}
